#!/bin/bash
# clang_format_check.sh — clang-format 格式检查（占位）
# 独立可跑：./build_script/clang_format_check.sh
# 返回 0（占位，不跑不失败）
#
# 设计见 doc/design/format.md。实现作为后续任务。
# 由 check.sh --filter clang-format 调用。

echo "clang-format check: skipped (not yet ready)."
echo "  Design: doc/design/format.md"
exit 0
