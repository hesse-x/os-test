#!/bin/bash
# clang_tidy_check.sh — clang-tidy 命名/静态检查（占位）
# 独立可跑：./build_script/clang_tidy_check.sh
# 返回 0（占位，不跑不失败）
#
# 设计见 doc/design/format.md（命名检查章节，增量强制）。实现作为后续任务。
# 由 check.sh --filter clang-tidy 调用。

echo "clang-tidy check: skipped (not yet ready)."
echo "  Design: doc/design/format.md"
exit 0
