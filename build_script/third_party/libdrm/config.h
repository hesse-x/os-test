/* config.h — hand-written replacement for meson auto-detection.
 * Not upstream; lives in third_party/drm/ for -include path convenience.
 * Consumed by xf86drm.c / xf86drmMode.c / libdrm_macros.h via -include
 * config.h.
 */
#define HAVE_VISIBILITY 1
#define MAJOR_IN_SYSMACROS 1
#define UDEV 0
#define _GNU_SOURCE 1
